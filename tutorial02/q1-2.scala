object Q1_2 extends App {

  // Q1
  var i, j, k = 2;
  var m, n = 5;
  var f = 12.0f; val g = 4.0f;
  var c = 'X';

  // Q2
  // a
  println(k + 12 * m);

  // b
  println(m / j);

  // c
  println(n % j);

  // d
  println(m / j * j);

  // e
  println(f + 10 * 5 + g);

  // f
  // println(++i * n); // ++i not a valid syntax
  i += 1;
  println(i * n);
}
